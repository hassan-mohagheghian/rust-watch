[package]
edition = "2024"
name = "collector"
version = "0.1.0"

[dependencies]
axum = {workspace = true}
protocol = {path = "../../shared/protocol"}
serde = {workspace = true}
tokio = {workspace = true}
